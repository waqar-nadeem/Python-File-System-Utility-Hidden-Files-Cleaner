# Python-File-System-Utility-Hidden-Files-Cleaner
This repository implements a Hidden Files Cleaner in Python that scans directories and removes hidden files and folders across Windows and Unix based systems. Concepts used include file system traversal (os.walk), OS detection, ctypes for Windows file attributes, path handling, and exception handling.
